#include <math.h>
#include <stdio.h>

long factorial(int num)

int main(int argc, char* argv[]){
    if (argc != 3){
        printf(stderr, "Error\n");
        exit(1);
    }
    
    printf("%ld\n", pow((int)argv[1], (int)argv[2])/)
}